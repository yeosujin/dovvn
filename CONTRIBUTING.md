# 개발 규칙

## 브랜치

`main`이 유일한 장기 브랜치이며 항상 배포 가능한 상태를 유지합니다. 모든 작업은 `main`에서 브랜치를 따서 진행하고 PR로 되돌립니다.

```
<prefix>/<간단한-설명>
```

설명은 영문 소문자 케밥 케이스로 씁니다. 예: `feat/clipboard-auto-paste`, `fix/tls-cert-verify`.

### prefix

[Conventional Commits](https://www.conventionalcommits.org/)의 타입을 그대로 씁니다.

| prefix      | 용도                                             |
| ----------- | ------------------------------------------------ |
| `feat/`     | 새 기능                                          |
| `fix/`      | 버그 수정                                        |
| `docs/`     | 문서만 변경                                      |
| `style/`    | 동작에 영향 없는 코드 포맷팅 (공백, 세미콜론 등) |
| `refactor/` | 동작 변경 없는 구조 개선                         |
| `perf/`     | 성능 개선                                        |
| `test/`     | 테스트 추가·수정                                 |
| `build/`    | 빌드 시스템, 의존성, 릴리즈 파이프라인           |
| `ci/`       | CI 설정                                          |
| `chore/`    | 위 어디에도 속하지 않는 잡무                     |
| `revert/`   | 이전 커밋 되돌리기                               |

`style`은 코드 포맷팅을 뜻합니다. Prettier가 자동 처리하므로 실제로 쓸 일은 거의 없습니다. **UI 디자인 변경은 `style`이 아니라 `feat` 또는 `fix`입니다.**

## 커밋 메시지

```
<type>: <요약>

<본문 — 왜 이렇게 했는지, 필요할 때만>
```

- `<type>`은 브랜치 prefix와 같은 목록을 씁니다.
- 요약은 한국어로, 마침표 없이 씁니다.
- scope는 쓰지 않습니다. 필요하면 요약에서 대상을 밝힙니다.
- 본문에는 무엇을 했는지보다 **왜 그렇게 했는지**를 남깁니다. 코드에서 읽히지 않는 판단 근거가 여기 들어갑니다.

릴리즈 커밋은 버전을 요약에 드러냅니다. 타입은 그 릴리즈의 대표 변경을 따릅니다.

```
feat: release v2.3.0 — 클립보드 URL 자동 붙여넣기
fix: release v2.3.1 — TLS 인터셉트 프록시 환경 인증서 검증 실패 수정
```

## Pull Request

작업 브랜치는 **squash merge**로 `main`에 합칩니다. squash 커밋 메시지는 PR 제목이 되므로, PR 제목도 커밋 메시지 규칙을 따릅니다.

머지 전 확인:

```bash
$ npm run typecheck
$ npm run lint
```

머지 후 작업 브랜치는 삭제합니다.

## CHANGELOG

사용자에게 보이는 변경은 `CHANGELOG.md`에 기록해야 릴리즈 노트에 반영됩니다. 기록하지 않으면 릴리즈 본문에서 누락됩니다.

- 새 버전은 파일 맨 위에 `## x.y.z` 섹션으로 추가하고 `- ` 불릿으로 적습니다.
- 사용자가 체감하는 표현으로 씁니다. 내부 구현 용어는 피합니다.
- 빌드·도구 변경처럼 노출하지 않을 내용은 같은 섹션 안 `### 내부` 아래에 둡니다. 이 부분은 릴리즈 노트에서 제외됩니다.

## 릴리즈

`main`에서 직접 진행합니다. 절차는 [README](README.md#릴리즈)를 참고하세요.
